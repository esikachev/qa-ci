- job-template:
    name: '{lab}-{mos}'
    defaults: global
    builders:
      - ci-config-prep
      - shell: |
              ssh 172.18.79.150 -l qa 'cd FuelDeployment/mos-components-ci/ && ./delete_vms.sh'
              ssh 172.18.79.150 -l qa 'cd FuelDeployment/mos-components-ci/ && ./launch.sh'
    publishers:
      - trigger-cleanup

- project:
    name: deploy_mos
    lab:
      - '151'
    mos:
      - '9.0'
    jobs:
     - '{lab}-{mos}'
