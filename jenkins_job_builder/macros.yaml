- builder:
    name: ci-config-prep
    builders:
      - shell: |
            git clone https://github.com/esikachev/qa-ci

- publisher:
    name: trigger-cleanup
    publishers:
      - trigger-parameterized-builds:
          - project: "integration-cleanup"
